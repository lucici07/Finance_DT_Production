# 使用说明

正式网址：https://finance-dt-weekly-plan.lucici1007.chatgpt.site/

## 首次连接

1. 打开本文件同目录下的 `.env.production`，复制 `OWNER_KEY=` 后面的值。
2. 打开正式网址，点击 **Connect cloud**，粘贴密钥并点击 **Connect**。
3. 如果提示加载云端工作区，选择确定。当前云端是空的，验收样例已经移除。

密钥具有完整读写权限，只供自己使用。转发给别人的内容请通过 **Share** 生成工作区链接（默认 Viewing，可切换 Editing）。

## 把旧网页里的数据搬过来

旧数据仍保存在你原来使用的浏览器中，不会自动跨网址迁移。

1. 在原来的浏览器打开 https://lucici07.github.io/Finance_DT_Production/ ，刷新后点击 **Connect cloud → Download full backup**，下载 JSON 备份。
2. 在新网址先按上面的步骤连接并加载云端工作区。
3. 点击 **Cloud settings → Restore full backup**，选择刚才下载的 JSON。确认替换本地数据。
4. 点击 **Save**，看到 **Synced** 和时间即表示已保存到云端。

不要在导入备份之后重新加载空的云端版本；顺序是先连接/加载，再导入，最后 Save。迁移完成前保留原浏览器数据和 JSON 备份。

## 日常使用

- 编辑完行或 Todo 后，点击顶部 **Save**，把全部周计划和 Calendar 数据存到云端。
- 点击 **Share** 并复制新链接。它打开完整工作区，包括所有周计划、Calendar 和 Dashboard，并保留分享时所在的视图。
- 接收者无需管理密钥，默认进入 **Viewing**；选择右上角 **Editing** 后可以修改，点击 **Save** 会更新你和其他人看到的同一份数据。转发链接的人也获得相同编辑能力。
- 他人保存后，页面在无未保存修改时每 15 秒自动读取更新；若有冲突会保留你的编辑并提示加载最新版本。
- 旧的单页只读链接继续保持原权限；请重新点击 Share 生成完整工作区链接。
- 共享页面未保存的修改只保留在当前标签页，请 Save 后再关闭。
- **Cloud settings → Revoke all share links** 可停用所有现有分享链接。
- 新设备/刷新页面后，重新输入管理密钥连接并加载已保存数据。
- 如出现版本冲突，先备份当前本地数据，再加载最新云端版本，合并你的修改后重新 Save。

上线验证已通过：公网 Save、匿名只读访问、默认查看、显式编辑、多人保存冲突与个人数据隔离、链接撤销、浏览器页面渲染。云端请求上限为 1.8 MB。

## Excel / PDF 导出

点击周计划工具栏的 **Export**，选择 Excel 或 PDF，也可以选择全部标签页或当前标签页。

- Excel：每个周计划标签页对应一个工作表。
- PDF：每个周计划标签页独占一张 A3 横向纸页；内容较多时自动缩小以完整放入。浏览器打印窗口打开后选择“另存为 PDF”，建议关闭浏览器页眉和页脚。
- 原有 Dashboard 的 Export PDF 功能继续保留。

## 手机访问

使用同一个网址，屏幕宽度不超过 760px 时会自动显示手机布局，无需安装 App。周计划以任务卡片显示，点击展开查看全部字段，再点击 Edit item 编辑；共享链接依然默认 Viewing，需要切换 Editing 后修改。底部导航可以切换 Weekly、Calendar、Dashboard 和 AI。更多工具收在 More tools 中，Export、Save 和 Share 保持可用。桌面宽屏自动恢复表格视图。
