# 使用说明

主入口：https://finance-dt-weekly-plan.lucici1007.chatgpt.site/

## Workspace views

顶部 Workspace views 栏用于切换独立工作区。每个 View 都有自己的 Weekly 分页、Calendar 和 Dashboard 数据，不是同一列表的筛选视图。
点击 + New view，输入名称，创建全新的空白云端工作区。
点击已有 View 切换。若当前有未保存修改，会先提示保存；取消或保存失败时留在当前工作区。
未保存的主入口草稿需要先点击 Save new workspace，再切换。
每个工作区会记住上次查看的周分页、日历或 Dashboard 选择。

原分享链接继续有效，打开后自动加入当前浏览器的 View 列表。Manage views 用于重命名、归类、移除本地快捷入口及导入导出目录。
View 列表和自定义名称目前保存在当前浏览器；任务内容独立保存在云端。目录不是所有用户共用的列表。
主入口仍打开默认空白模板，不自动加载个人内容。

## 查看、编辑与同步

分享链接默认 Viewing。切换到 Editing 后可修改内容。
单击单元格直接输入，双击打开整行编辑窗口。Enter 确认，Esc 取消，Tab 切换列。
空白行可直接输入新增任务。No. 和自动更新时间保持只读。

顶部同步小方块：
- 绿色：当前内容已同步。
- 黄色：有未完成编辑、未保存修改或正在同步，点击 Save 保存。
- 红色：同步失败或访问异常，查看提示后处理。

云端会定期检查其他人的已保存修改。同步方块也可点击以重新加载云端版本。
若有未保存内容，手动重新加载会明确询问是否放弃这些修改，取消可保留。
Save 将当前修改上传云端；状态绿色后才表示此次保存成功。
不再提供 Download backup 按钮。Excel/PDF 导出仍在 Export 中。

## 页面与已有数据

W1 和 Next 3 Weeks 不能删除或重命名。
在目标单元格粘贴 Excel 多行，Next 3 Weeks 的粘贴内容保留在该页。
右键 Clear this page 只清空当前页，支持 Undo 撤销。
旧 JSON 备份仍可通过主入口 Import backup 导入，再另存为新的工作区。
旧浏览器数据不会自动加载或删除。

## 手机

同一链接自动适配手机，顶部 Workspace views 可横向滚动。
手机任务使用展开卡片编辑；底部导航切换 Weekly、Calendar 和 Dashboard。
