{
  "name": "finance-dt-plan",
  "private": true,
  "version": "1.1.0",
  "engines": {
    "node": ">=24.14.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node build-site.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
